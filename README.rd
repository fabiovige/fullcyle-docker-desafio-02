# Nome do Projeto

Este projeto configura um ambiente Docker com Nginx, Node.js e MySQL. O Nginx atua como proxy reverso para a aplicação Node.js que se conecta ao banco de dados MySQL.

## Iniciar o Projeto

Para iniciar o projeto, execute o seguinte comando no diretório raiz:

```bash
docker-compose up -d


